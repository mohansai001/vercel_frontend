import React from 'react';
// Placeholder for chart, integrate with chart.js or recharts as needed
const Chart = ({ data, options }) => (
  <div style={{ minHeight: 200, textAlign: 'center', color: '#888' }}>
    Chart Placeholder
  </div>
);
export default Chart;
